# Dagster instance configuration
# Documentation: https://docs.dagster.io/deployment/dagster-instance

# unified storage for runs, event logs, and schedules
storage:
  sqlite:
    # use environment variable so runtime path is configurable
    base_dir:
      env: DAGSTER_STORAGE_DIR

# logs stored on local filesystem
compute_logs:
  module: dagster.core.storage.local_compute_log_manager
  class: LocalComputeLogManager
  config:
    # use environment variable so runtime path is configurable
    base_dir:
      env: DAGSTER_LOGS_DIR

run_coordinator:
  module: dagster.core.run_coordinator
  class: QueuedRunCoordinator
  config:
    max_concurrent_runs: 3 # safe default to avoid SIGBUS error

# enable run monitoring for better error detection
run_monitoring:
  enabled: true

# disable telemetry
telemetry:
  enabled: false
