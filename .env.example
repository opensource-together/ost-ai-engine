# This file serves as an example for the required environment variables.
# Copy this file to .env and fill in your actual values.

# --- PostgreSQL Database ---
# The connection string for your PostgreSQL database.
# Format: postgresql://<user>:<password>@<host>:<port>/<database_name>
DATABASE_URL=""

# --- Redis Server ---
# The connection string for your Redis server, used by Celery.
# Format: redis://<@host>:<port>
REDIS_URL="redis://localhost:6379/0"

# --- GitHub API Access ---
# Your personal GitHub access token for scraping repositories.
# This is required by the populate_db.py script.
GITHUB_ACCESS_TOKEN=""

# --- Model Storage ---
# (Optional) The directory where trained model artifacts will be saved.
# Defaults to "models/" if not set.
MODEL_DIR="models/"

# --- Optional: GitHub Scraper Configuration ---
# These variables are used by the scripts/populate_db.py script.
# You can either provide a list of specific repos OR use search queries.

# A comma-separated list of specific repositories to fetch (e.g., "owner/repo1,owner/repo2")
GITHUB_REPO_LIST=""

# (Alternative to the list) A query to find popular Python repositories.
#GITHUB_PYTHON_QUERY="language:python stars:>2000"

# (Alternative to the list) A query to find popular JavaScript repositories.
#GITHUB_JS_QUERY="language:javascript stars:>2000"