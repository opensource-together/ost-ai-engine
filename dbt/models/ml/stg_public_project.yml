version: 2

models:
  - name: stg_public_project
    description: "Final staging model for ML. Cleans the generated context from raw_public_project."
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: context
        description: "Cleaned semantic context ready for embedding."
        tests:
          - not_null
