
version: 2

models:
  - name: stg_public_category
    description: Staging model for `public.Category` reference data. Used to normalize user interest categories.
    columns:
      - name: id
        description: Primary key
      - name: name
        description: Category name
      - name: created_at
        description: Timestamp of creation
