version: 2

models:
  - name: stg_public_user
    description: "Staging model cleaning raw user data from public.user"
    columns:
      - name: user_id
        description: "Primary key (UUID)"
        tests:
          - unique
          - not_null
      - name: name
      - name: email
      - name: job_title
      - name: bio
      - name: github_username
      - name: created_at
      - name: updated_at
