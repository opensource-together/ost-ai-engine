version: 2

models:
  - name: pvt_public_user
    description: "Pivot model formatting user data into a single context string for embedding"
    columns:
      - name: user_id
        tests:
          - unique
          - not_null
      - name: user_context
        description: "Enriched text representation of the user profile"
