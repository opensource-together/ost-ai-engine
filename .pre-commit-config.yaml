# Requires: uv sync (dev group). Install hooks: uv run pre-commit install
repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: uv run ruff check
        language: system
        types_or: [python, pyi]
      - id: ruff-format
        name: ruff format (check)
        entry: uv run ruff format --check
        language: system
        types_or: [python, pyi]
      - id: mypy
        name: mypy
        entry: uv run mypy src/
        language: system
        pass_filenames: false
      - id: sqlfluff
        name: sqlfluff lint
        entry: uv run sqlfluff lint dbt/
        language: system
        pass_filenames: false
